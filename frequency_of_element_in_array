def frequency():
    arr = list(map(int, input().split()))
    x = int(input())
    return arr.count(x)

print(frequency())

# to get frequency of all elements in an array
#arr = list(map(int, input().split()))

#for i in arr:
#    print(i, arr.count(i))